%h2 Edit task

= simple_form_for @task do |f|
  %fieldset
    = f.input :name
    = f.input :priority
    = f.input :content, :as => :text
  .actions
    = f.submit "Save", :class => "btn primary"
    = link_to "Delete", task_path(@task), :class => "btn"
