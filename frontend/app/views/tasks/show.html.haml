= link_to "<< Back to tasks", tasks_path

%h2
  Task:
  = @task.name

%h3
  Priority:
  = @task.priority

%p= @task.content

= link_to "Edit task", edit_task_path(@task), :class => "btn"
= form_tag task_path(@task), :method => :delete do
  = submit_tag "Delte task", :class => "btn danger"
